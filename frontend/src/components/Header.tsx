import React from "react"
import styles from "../assets/Header.module.scss"

const Header: React.FC = () => {
  return (
    <div className={styles.wrapper}>
      <h1>SUPERMARKET COMPARISON</h1>
    </div>
  )
}

export default Header
